# Environment variables for backend
GEMINI_API_KEY=your_gemini_api_key_here

# LLM Generator Configuration
# Set to "true" to enable LLM-based pictogram selection (uses Gemini Flash-lite)
# Set to "false" to use only embedding-based selection
USE_LLM_GENERATOR=true
